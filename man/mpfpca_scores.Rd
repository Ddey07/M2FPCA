% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scores.R
\name{mpfpca_scores}
\alias{mpfpca_scores}
\title{Multivariate principal-component scores from an mpfpca.dir fit}
\usage{
mpfpca_scores(
  dat_list,
  type,
  mpf,
  argvals,
  npc = NULL,
  ridge = 0.05,
  zcap = 8,
  impute.missing = FALSE
)
}
\arguments{
\item{dat_list}{List of \code{p} numeric matrices (n by m), as passed to \code{\link[=mpfpca.dir]{mpfpca.dir()}}.}

\item{type}{Character vector of length \code{p} of variable types.}

\item{mpf}{Output of \code{\link[=mpfpca.dir]{mpfpca.dir()}}.}

\item{argvals}{Numeric vector of length m of argument values (must match the fit).}

\item{npc}{Number of components to score; defaults to \code{mpf$L}.}

\item{ridge}{Optional ridge passed to \code{getLatentPreds} if the installed
SGCTools supports it (stabilises the per-subject BLUP solve for poorly
estimated margins). Ignored otherwise.}

\item{zcap}{Winsorising cap on the magnitude of latent predictions
(\code{Inf} disables); guards against rare blow-ups before projection.}

\item{impute.missing}{Logical, passed to \code{getLatentPreds}.}
}
\value{
A list with
\describe{
\item{scores}{list of length \code{npc}; \code{scores[[l]]} is an n-by-p matrix of
the l-th component scores for each variable}
\item{scores_wide}{an n-by-(npc*p) matrix, components grouped}
\item{latent}{list of \code{p} latent-trajectory matrices (n by m)}
}
}
\description{
Computes subject-level multivariate FPC scores by (1) predicting each
variable's latent continuous trajectory with \code{\link[SGCTools:getLatentPreds]{SGCTools::getLatentPreds()}} and
(2) projecting those trajectories onto the (shared) temporal eigenfunctions
from an \code{\link[=mpfpca.dir]{mpfpca.dir()}} fit:
\eqn{\theta_{i v \ell} = \int L_{v i}(t)\,\phi_\ell(t)\,dt}.
}
\references{
Dey, D., Ghosal, R., Merikangas, K., & Zipunnikov, V. (2026)
"Multivariate Functional Principal Component Analysis for Mixed-Type mHealth
Data: An Application to Mood Disorders." \emph{arXiv:2603.11385}.
\url{https://arxiv.org/abs/2603.11385}
}
